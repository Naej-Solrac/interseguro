// DTO de salida del caso de uso basado en la entidad de dominio.
import { MatrixStatistics as DomainMatrixStatistics } from '../../domain/entities/matrix-statistics.entity';

export type MatrixStatistics = DomainMatrixStatistics;
